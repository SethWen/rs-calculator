#![feature(coroutines, coroutine_trait, stmt_expr_attributes)]
#![allow(unused)]

use std::collections::{LinkedList, VecDeque};

mod fatptr_example;
mod libc_example;
mod pin_example;
mod sync_example;

fn main() {
    println!("Hello, world!");
}
